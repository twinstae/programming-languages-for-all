---
title: Kotlin
description: A guide in my new Starlight docs site.
sidebar:
  order: 8
  hidden: true
---

import { LinkCard } from '@astrojs/starlight/components';
import { YouTube } from 'astro-embed';

<div class="not-content">
  <YouTube id="https://www.youtube.com/watch?v=xT8oP0wy-A0&t=12s" />
</div>

```elixir
IO.puts("Hello, World!")
```

```elixir
defmodule Fact do
  def factorial(n) when is_integer(n) and n > 0 do
    case n do
      0 -> 1
      1 -> 1
      _ ->  n * factorial(n - 1)
    end
  end
end

IO.puts("enter a number:")

number = case Integer.parse(IO.read(:stdio, :line)) do
    :error -> raise "Invalid input. Please enter a valid non-negative integer."
    {n, _} when n < 0 -> raise "Invalid input. Please enter a non-negative integer."
    {n, _} -> n
end

IO.puts(Fact.factorial(number))
```
